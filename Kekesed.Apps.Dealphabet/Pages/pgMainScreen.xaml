<Page x:Class="pgMainScreen"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      d:DesignHeight="529.663" d:DesignWidth="623.125"
      Title="pgMainScreen">

    <Grid>
        <StackPanel 
            Orientation="Horizontal"
            Background="{DynamicResource Brush01}"
            VerticalAlignment="Top"
            HorizontalAlignment="Stretch" d:IsLocked="True">
            <TextBlock
                FontWeight="Thin"
                Margin="10"
                FontSize="15"
                Foreground="{DynamicResource Brush02}">
                <Span>Kekesed</Span>
                <Span FontWeight="Medium">Dealphabet</Span>
            </TextBlock>
             
            <Menu VerticalAlignment="Center" HorizontalAlignment="Stretch">
                <MenuItem Header="About" Click="mnuAbout_Click"/>
            </Menu>
        </StackPanel>
        <Frame Margin="0,40,0,0" Source="pack://application:,,,/Pages/Client/pgEnter.xaml" NavigationUIVisibility="Hidden"/>
    </Grid>
</Page>
