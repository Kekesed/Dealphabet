<Page x:Class="pgResult"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="330.35" d:DesignWidth="520.623"
      Title="pgResult">
    <ScrollViewer ScrollViewer.VerticalScrollBarVisibility="Visible" Padding="10" >
        <StackPanel>
            <Label Content="Alphabet/Dealphabet" Tag="H3" Margin="0,3,0,0"/>
            <TextBox x:Name="txtResult" 
                MinLines="7"
                IsReadOnly="True"
                TextWrapping="WrapWithOverflow" 
                AcceptsReturn="True" 
                VerticalAlignment="Top"/>
            <Button Content="Back..." Width="100" HorizontalAlignment="Left" Margin="10,20" Click="Button_Click"/>
        </StackPanel>

    </ScrollViewer>
</Page>
